<br>
<hr>
<br>

<h2 id="citation">Citation</h2>

If you would like to cite this article, please use:

{% assign citation_author = page.author | default: site.last_name %}

{% capture citation_quote -%}
> {{ citation_author }} ({{ page.date | date: "%b %Y" }}). {{ page.title }}. {% if site.title != 'blank' %}{{ site.title }}. {% endif %}{{ site.url }}{{ page.url }}.
{%- endcapture %}
{{ citation_quote | markdownify }}

<p>Or copy the BibTeX entry:</p>

{% capture citation_code -%}
```bibtex
@article{ {{- page.citation_key | default: page.slug | prepend: "wang2026" -}},
  title   = { {{- page.title -}} },
  author  = { {{- citation_author -}} },
  {%- if site.title != 'blank' %}journal = { {{- site.title -}} },{% endif %}
  year    = { {{- page.date | date: "%Y" -}} },
  month   = { {{- page.date | date: "%b" -}} },
  url     = { {{- site.url }}{{ page.url -}} }
}
```
{%- endcapture %}
{{ citation_code | markdownify }}
