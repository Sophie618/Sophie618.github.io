---
layout: default
---
<div class="post">
  <header class="post-header">
    <h1 class="post-title">
      {% if site.title == 'blank' %}
        <span class="font-weight-bold">
          {{ site.first_name -}}
          {%- if site.middle_name != blank %} {{ site.middle_name }}{% endif %}
          {{ site.last_name }}
        </span>
      {% else %}
        {{ site.title }}
      {% endif %}
    </h1>
    <p class="desc">{{ page.subtitle }}</p>
  </header>

  <article>
    {% if page.profile %}
      <div class="profile float-{% if page.profile.align == 'left' %}left{% else %}right{% endif %}">
        {% if page.profile.image %}
          {% assign profile_image_path = page.profile.image | prepend: 'assets/img/' %}
          {% if page.profile.image_circular %}
            {% assign profile_image_class = 'img-fluid z-depth-1 rounded-circle' %}
          {% else %}
            {% assign profile_image_class = 'img-fluid z-depth-1
      rounded' %}
          {% endif %}
          {% capture sizes %}(min-width: {{ site.max_width }}) {{ site.max_width | minus: 30 | times: 0.3}}px, (min-width: 576px)
      30vw, 95vw"{% endcapture %}
          {%
            include figure.liquid loading="eager" path=profile_image_path class=profile_image_class sizes=sizes alt=page.profile.image
            cache_bust=true
          %}
        {% endif %}
        {% if page.profile.more_info %}
          <div class="more-info">{{ page.profile.more_info }}</div>
        {% endif %}
        {% if page.profile.social_icons %}
          <div class="profile-social-icons" aria-label="Contact and academic profiles">
            <div class="profile-contact">
              <button
                id="profile-email-button"
                class="profile-icon-button"
                type="button"
                title="Email"
                aria-label="Show email addresses"
                aria-expanded="false"
                aria-controls="profile-email-popover"
              >
                <i class="fa-solid fa-envelope"></i>
              </button>
              <div
                id="profile-email-popover"
                class="profile-contact-popover email-options"
                role="dialog"
                aria-label="Email addresses"
                hidden
              >
                {% for email in site.data.socials.emails %}
                  <a href="mailto:{{ email.address }}">
                    <span>{{ email.label }}</span>
                    <small>{{ email.address }}</small>
                  </a>
                {% endfor %}
              </div>
            </div>
            {% if site.data.socials.x_url %}
              <a href="{{ site.data.socials.x_url }}" title="X" aria-label="X" target="_blank" rel="noopener noreferrer">
                <i class="fa-brands fa-x-twitter"></i>
              </a>
            {% else %}
              <span class="is-pending" title="X profile link coming soon" aria-label="X profile link coming soon">
                <i class="fa-brands fa-x-twitter"></i>
              </span>
            {% endif %}
            {% if site.data.socials.github_username %}
              <a
                href="https://github.com/{{ site.data.socials.github_username }}"
                title="GitHub"
                aria-label="GitHub"
                target="_blank"
                rel="noopener noreferrer"
              >
                <i class="fa-brands fa-github"></i>
              </a>
            {% endif %}
            {% if site.data.socials.google_scholar_url %}
              <a
                href="{{ site.data.socials.google_scholar_url }}"
                title="Google Scholar"
                aria-label="Google Scholar"
                target="_blank"
                rel="noopener noreferrer"
              >
                <i class="ai ai-google-scholar"></i>
              </a>
            {% else %}
              <span class="is-pending" title="Google Scholar profile link coming soon" aria-label="Google Scholar profile link coming soon">
                <i class="ai ai-google-scholar"></i>
              </span>
            {% endif %}
          </div>
        {% endif %}
      </div>
    {% endif %}

    <div class="clearfix">{{ content }}</div>

    <!-- News -->
    {% if page.announcements and page.announcements.enabled %}
      <h2>
        <a href="{{ '/news/' | relative_url }}" style="color: inherit">news</a>
      </h2>
      {% include news.liquid limit=true %}
    {% endif %}

    <!-- Latest posts -->
    {% if page.latest_posts and page.latest_posts.enabled %}
      <h2>
        <a href="{{ '/blog/' | relative_url }}" style="color: inherit">latest posts</a>
      </h2>
      {% include latest_posts.liquid %}
    {% endif %}

    <!-- Selected papers -->
    {% if page.selected_papers %}
      <h2>
        <a href="{{ '/publications/' | relative_url }}" style="color: inherit">selected publications</a>
      </h2>
      {% include selected_papers.liquid %}
    {% endif %}

    <!-- Social -->
    {% if page.social %}
      <div class="social">
        <div class="contact-icons">{% social_links %}</div>

        <div class="contact-note">{{ site.contact_note }}</div>
      </div>
    {% endif %}

    {% if site.newsletter and site.newsletter.enabled and site.footer_fixed %}
      {% include newsletter.liquid center=true %}
    {% endif %}
  </article>
</div>

{% if page.profile.social_icons %}
  <script defer src="{{ '/assets/js/profile-contacts.js' | relative_url | bust_file_cache }}"></script>
{% endif %}
